import { teal } from "@material-ui/core/colors";

const styles = {
  card: {
    background: teal[500],
    padding: "2rem"
  },
  title: {
    color: "white",
    textAlign: "center"
  }
};

export default styles;
